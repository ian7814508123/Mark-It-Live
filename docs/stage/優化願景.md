1. 強化離線體驗 (PWA): 這類工具最適合在飛機上、咖啡廳 等網路不穩的環境下使用。
- (目前瀏覽器預先載入並且不要重新整理就能有類離線的體驗)
* Web Share Target API： 讓手機端使用者可以從其他 App「分享文字或檔案」直接進入編輯器。

* File System Access API： 這是 Serverless 應用的殺手鐧。讓使用者可以直接「開啟」並「同步」電腦硬碟裡的 .md 檔案，而不只是下載複本後再貼上。這樣編輯器就變成了真正的本地桌面軟體替代品。

2. 靜態資源的「邊緣化」優化: 由於沒有伺服器處理邏輯，預覽渲染（如 Mermaid, KaTeX, Vega）的 JS 包通常很大。

* 動態載入 (Code Splitting)： 只有當 Markdown 偵測到有 mermaid 語法時，才去載入 Mermaid 的庫。

* Web Workers 渲染： 像 Mermaid 或複雜的 LaTeX 渲染會占用主線程導致打字卡頓。將渲染邏輯丟進 Web Worker，確保打字輸入始終保持 60fps。

3. 數據遷移與雲端橋接 (無伺服器同步): 使用者最怕「換一台電腦資料就沒了」。

* RemoteStorage 或 WebDAV 支援： 讓使用者填入自己的 Dropbox 或自建的 Nextcloud 連結，實現「無伺服器同步」。

* URL State Sharing： 將較短的 Markdown 內容直接編碼進 URL（使用 LZ-compression），這樣使用者傳一個連結給朋友，對方點開就能看到內容，完全不需要資料庫。

4. 編輯器深度功能 (UX 提升)
* 圖床自動化（免伺服器版）： 整合 Imgur 或 Cloudinary 的 API。使用者貼上圖片後，前端直接呼叫 API 上傳並回傳連結，保持應用 Serverless 的特性。

* 全文檢索 (Full-text Search)： 既然資料都在本地 IndexedDB，可以使用 FlexSearch 或 MiniSearch 在前端建立索引，實現瞬間搜尋所有文檔的功能。

5. 導出品質強化
* 自定義 CSS 主題： 提供幾套預設的 PDF 導出樣式（如：學術風、簡約風、Github 風），並定期發佈新樣板以增進品牌互動。

6. 多媒體與檔案管理
* 雲端同步 / GitHub Gist 整合： 目前看起來是 Local-first (IndexedDB)。如果能增加「發佈至 Gist」或同步到 Google Drive / Dropbox，能大幅提升跨裝置使用的便利性。

* 拖放圖片自動轉 Base64/上傳： 目前圖片可能需要外部連結，若能支援直接拖入圖片並自動處理（或串接 Imgur API），會是很大的加分項。

7. 更多專業領域強化
* 更強大的化學/物理支援： 目前已有 mhchem，可以考慮加入 CircuitTikZ（電路圖）或更豐富的 Biomedical 繪圖支持。

* 簡報模式 (Slide Mode)： 支援 --- 分隔線自動分頁，並提供一個「投影片預覽」模式（類似 Marp 或 Reveal.js），這對技術分享非常有用(不過這要同時優化目前的列印預覽功能)。

8. 致謝名單 UX再設計
* 1. 巢狀分類：
    1. 依性質分類: 區分「開源第三方套件 (Open Source Libraries)」、「設計與資產 (Icons / Fonts / Graphics)」、「核心貢獻者 (Contributors)」與「贊助商/合作夥伴 (Sponsors)」。
    2. 依核心度分類：主要框架（如 React、Vue）會獨立凸顯，次要的工具型套件則歸類到進階列表中。
* 2. 多欄位網格與卡片化佈局: 當名單達到一定數量時，垂直條列式會造成嚴重的「捲動疲勞」。


🚀 優先級建議

| 優先級 | 項目 | 原因 |
| :--- | :--- | :--- |
| High | 自定義 CSS 主題 | 實作難度低且價值高，能提升專業感與品牌辨識度。 |
| Medium | Web Workers 渲染 | 解決大文件或複雜圖表造成的打字延遲。 |
| Medium | 全文檢索 | 當使用者文檔變多時（50 份以上），搜尋是剛需。 |
| Low | 數據同步 | 跨裝置同步雖非核心，但能增加黏著度。 |

---

- Q. mermaid官方作的反向編輯功能, 你覺得我們有辦法做得出來嗎?
- 發現: 畫面左側: mermaid code; 右側: 畫布 , 使用者可以直接在節點上按"+"新增節點, 左側code也會對應新增語法

- 可能原理: 像我們之前用AST做Github alert, code <---> 中間人(結構化物件) <---> Canvas 做雙向溝通

- 使用技術: 
1. 抽象語法樹: 將 Mermaid 文本程式碼解析為結構化的物件(JSON)。
2. 單一資料源: 程式碼與畫布狀態共享同一個底層數據模型。
3. 雙向數據綁定與事件監聽。
    1. 畫布上的操作：當你在畫布上點擊 + 號、拖曳連線，或者修改節點名稱時，前端框架（如 React/Vue）會捕捉到這個滑鼠或觸控事件。
    2. 直接修改資料模型：例如，在 C 節點下點擊「新增分支」，畫布的事件處理器（Event Handler）會直接在底層的 JSON 物件中，對 edges 和 nodes 陣列進行 push 操作。
4. 程式碼生成器：將修改後的 AST 重新轉譯回 Mermaid 語法。
    1. 反向轉譯器: 當底層的 JSON 資料因為使用者在畫布上的操作而改變後，系統會觸發一個`程式碼生成器`。
        - 規則對應：`程式碼生成器`裡面寫滿了 Mermaid 語法的規則。
            - 如果偵測到某個 node 的 shape 是 round，它就知道輸出時要用 () 包裹。
            - 如果偵測到一條線有 text，它就知道要輸出 -->|text|。
    2. 重新字串化：`程式碼生成器`會遍歷整棵修改後的 JSON 樹，將其重新組裝成標準的 Mermaid 字串，並直接覆蓋掉左側編輯器的文字。
5.  保持程式碼排版: 考慮到許多人習慣手寫 Mermaid 並加入註解或調整順序，若直接覆蓋會破壞原有的排版與註解。        
    1. 增量更新 (Incremental Updates)：只修改受影響的那一行程式碼，保留其他行和註解的原貌。
    2. 源映射 (Source Maps)：在解析時，JSON 的每個節點都會記錄它對應到左側文字的「第幾行、第幾個字元」。當你在畫布點擊 C 節點時，系統能精準定位到左側第 3 行，進行局部替換。

目前兩個模式下都能透過點擊建立節點了(但狀態稍微有點不穩定), 
1. mermaid 模式: 切換至編輯模式後新增節點ok, 但當我轉去操作其他東西(如. 改成編輯code、按zoom in/out按鈕、來回切換拖曳/編輯)後就沒辦法再透過點擊新增節點
---
我們先來掃描編輯模式下會因為同時操作其他元件, 或是來回切換拖曳/編譯 後,造成編輯模式失效的可能原因
1. 元件被「銷毀並重新掛載」
常見場景：
上層元件使用了條件渲染（例如 isSidebarOpen && <Editor />），操作其他元件時不小心讓條件短暫變成 false 又變回 true。
在渲染函數內部「動態宣告」了新元件, 就會導致斷鍵
2. 狀態未提升或被上層狀態覆蓋
原因：編輯模式狀態如果是從 props 傳下來，或者依賴於某個上層的資料源。當操作其他元件時，觸發了資料重新獲取或父元件更新，父元件傳下來了舊的、或是重設後的資料，編輯狀態就被蓋掉了。
---

接下來是我去參考mermaid官方在UI編輯上的可調整性:
1. 我們可以就目前已有的HUD進行升級(如圖. 分成頂列工具欄 + (選中節點後)快捷工具欄功能
發現: 
1. 官方的UX很爛, 選擇畫面上比較高的節點,兩種工具欄就會疊在一起, 所以可以考慮的原則就是一次只出現一種工具欄(並直接透過HUD切換)
2. 我有整理功能具體內容, 並且分清楚哪些可加/可不加, 就可以再細部討論HUD的設計
3. 希望我們周邊的UI組件提升後, 能夠回頭優化目前新增節點時遇到的Bug(或是先修bug再提升)

- 頂列工具列包含:
1. 切換Pan(很單純的Pan on/off)
2. 選擇節點主題(這個不用, 已經有css style)
3. 選擇節點形狀(例如.圓/圓角方形/菱形...等流程圖常用節點)
4. 添加icon 5. 添加圖片 (4.5 用途未知可以先不用)
6. 添加文字方塊
7. 添加子圖subgraph
8. auto-layout
9. 排列方向(依附於8開啟時):上到下/下到上/左到右/右到左
10. layout: 層級式hierarchical/ 自適應adaptive(看起來像是主幹固定於同一邊, 分支往另一邊長出去)
- HUD可以考慮組件化, 這樣markdown模式可能也可以叫用
- 目前HUD還沒加undo/redo的功能, 可以刪除Cursor(x,y)
- 要更清楚定義甚麼時後展開/收合(不再只是像現在點擊展開/點擊空白收合, 有可能是本來很多功能, 展開後多出視覺提升的非必要工具)

- 快捷工具列包含:
1. 形狀(個別改變節點形狀用)
2. background style
3. border style
4. textstyle
5. apply style(就是234直接包一組範本快速套用, 可以不用)
- 234可以同時結合到之前整合了主題系統與 Mermaid 配色。讓使用者可以在 Markdown 文檔中直接內嵌 `<style>` 標籤，或在客製化主題中修改以下 CSS 變數，即時微調圖表外觀(可能要加入思考要怎麼把樣式變數接口配合AST達到雙邊同步編輯)
6. 添加超連結(有輸入的GUI:text area/save/cancel)
7. 移動至子圖(有GUI:選擇的清單/沒有子圖就提醒新增)
8. Duplicate
9. Go to code(他會直接指左邊的程式碼)
10. delete